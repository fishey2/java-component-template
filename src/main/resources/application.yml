spring:
  main:
    banner-mode: off
  profiles:
    active: "dev"
  swagger:
    endpoint: /swagger-ui.html

---

spring:
  profiles: dev
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin

---

spring:
  profiles: test
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin

---

spring:
  profiles: prod
  activemq:
    broker-url: tcp://activemq:61616
    user: admin
    password: admin